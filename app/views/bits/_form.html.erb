<%= form_with(model: bit, local: true) do |form| %>
  <% if bit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bit.errors.count, "error") %> prohibited this bit from being saved:</h2>

      <ul>
      <% bit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :profile_id %>
    <%= form.number_field :profile_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
